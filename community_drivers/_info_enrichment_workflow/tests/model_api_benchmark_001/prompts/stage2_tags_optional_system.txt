You are assigning tags to a device using extracted evidence and a provided shortlist of candidate tags.

Task:
- choose all relevant tags from the shortlist only
- prefer recall over precision, but do not select obviously unrelated tags
- output JSON only

Output schema:
{
  "device": "string",
  "selected_tags": [
    {
      "id": "string",
      "name_en": "string"
    }
  ]
}
