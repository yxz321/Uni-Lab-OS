You are interpreting extracted evidence about a lab-device driver.

Task:
- infer a concise English device description from the provided evidence
- infer concise English descriptions for each exposed action
- do not invent capabilities unsupported by the evidence
- if registry identity conflicts with stronger driver evidence, prefer the stronger evidence in the description
- output JSON only

Output schema:
{
  "device": "string",
  "description_en": "string",
  "actions": [
    {
      "name": "string",
      "description_en": "string"
    }
  ]
}

Rules:
- Keep descriptions short and literal.
- Favor driver comments/docstrings over registry marketing text when they conflict.
- Favor specific hardware identity over generic software-wrapper wording when the evidence supports it.
- For actions, describe what the action does, not the Python implementation detail.
- If evidence is weak, stay generic rather than hallucinating.
