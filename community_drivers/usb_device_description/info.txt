usb_device_description:
  name: USB设备连通性诊断组件
  name_en: USB Device Connectivity Diagnostic Component
  manufacturer: ''
  category:
  - 实验室仪器枚举器
  - USB仪器通信接口
  - 串行仪器通信接口
  tags:
  - 实验执行&合成设备
  - 实验室自动化与仪器集成
  - 仪器发现与驱动匹配
  - 实验仪器通信与驱动集成
  - 实验室仪器枚举器
  - USB仪器通信接口
  - 串行仪器通信接口
  description: 这是一个非实体软件组件，用于在实验系统中描述USB设备并执行连接诊断。它主要检查设备是否可见、是否映射为POSIX端口、端口路径是否有效、端口能否打开，以及是否能够进行命令读写，以帮助排查仪器与主机之间的USB连接问题。
  description_en: This is a non-physical software component used in laboratory systems
    to describe USB devices and perform connectivity diagnostics. It checks whether
    a device is visible, whether it appears as a POSIX port, whether port paths are
    valid, whether ports can be opened, and whether command read/write is possible,
    helping troubleshoot USB connections between instruments and host computers.
  class:
    action_value_mappings:
      auto-isVisible:
        schema:
          description: 检查目标USB设备是否被系统识别并可见。
          description_en: Check whether the target USB device is detected and visible
            to the system.
      auto-isVisibleOnUSBHub:
        schema:
          description: 检查目标设备是否在USB集线器枚举中可见。
          description_en: Check whether the target device is visible in USB hub enumeration.
      auto-diagnoseConnectivity:
        schema:
          description: 执行USB设备与端口的连通性诊断。
          description_en: Run connectivity diagnostics for the USB device and its
            ports.
      auto-isVisibleAsPOSIXPort:
        schema:
          description: 检查设备是否以POSIX端口形式出现在系统中。
          description_en: Check whether the device appears in the system as a POSIX
            port.
      auto-isValidPOSIXPath:
        schema:
          description: 验证POSIX设备路径是否有效。
          description_en: Validate whether the POSIX device path is valid.
      auto-hasUniquePOSIXPortMatch:
        schema:
          description: 检查是否能唯一匹配到一个POSIX端口。
          description_en: Check whether there is a unique matching POSIX port.
      auto-bsdPath:
        schema:
          description: 获取或解析对应的BSD设备路径。
          description_en: Get or resolve the corresponding BSD device path.
      auto-bsdPathMatches:
        schema:
          description: 检查BSD设备路径是否与预期匹配。
          description_en: Check whether the BSD device path matches the expected value.
      auto-portCanBeOpened:
        schema:
          description: 检查已识别端口是否可以打开。
          description_en: Check whether the identified port can be opened.
      auto-usbPortCanBeOpened:
        schema:
          description: 检查USB端口是否可以打开。
          description_en: Check whether the USB port can be opened.
      auto-usbPort:
        schema:
          description: 获取USB端口标识或端口信息。
          description_en: Get the USB port identifier or port information.
      auto-posixPort:
        schema:
          description: 获取对应的POSIX端口路径或标识。
          description_en: Get the corresponding POSIX port path or identifier.
      auto-posixPortCanBeOpened:
        schema:
          description: 检查POSIX端口是否可以打开。
          description_en: Check whether the POSIX port can be opened.
      auto-canReadWritePOSIXCommands:
        schema:
          description: 检查是否可通过POSIX端口进行命令读写。
          description_en: Check whether commands can be read and written through the
            POSIX port.
      auto-canReadWriteUSBCommands:
        schema:
          description: 检查是否可通过USB接口进行命令读写。
          description_en: Check whether commands can be read and written through the
            USB interface.
      auto-assertTrue:
        schema:
          description: 断言某项检查结果为真。
          description_en: Assert that a diagnostic property or check result is true.
      auto-assertFalseOrRaise:
        schema:
          description: 断言某项检查结果为假，否则抛出异常。
          description_en: Assert that a diagnostic property or check result is false;
            otherwise raise an exception.
      auto-assertRaise:
        schema:
          description: 断言某项检查应触发异常。
          description_en: Assert that a diagnostic check is expected to raise an exception.
      auto-assertEqual:
        schema:
          description: 断言某项检查结果等于期望值。
          description_en: Assert that a diagnostic result equals the expected value.
      auto-assertNotEqual:
        schema:
          description: 断言某项检查结果不等于期望值。
          description_en: Assert that a diagnostic result does not equal the expected
            value.
      auto-report:
        schema:
          description: 生成USB设备连通性诊断报告。
          description_en: Generate a USB device connectivity diagnostic report.
auto_annotation_metadata:
  registry_key: usb_device_description
  annotation_workflow_version: v4
  tag_hints:
  - USB connectivity diagnostics
  - POSIX serial port detection
  - device enumeration
  - port open test
  - USB command I/O check
  - instrument connection troubleshooting
  existing_tags:
  - id: '4313'
    name: 实验执行&合成设备
    name_en: Experiment Execution & Synthesis Equipment
    type: experimental_step
    rationale: 该诊断组件用于实验系统中检查设备可见性、端口可开性与命令读写，属于实验执行前后的联机诊断支撑。
  - id: P-0001
    name: 实验室自动化与仪器集成
    name_en: Laboratory Automation & Instrument Integration
    type: experimental_domain
    rationale: 它面向实验系统中的 USB 仪器连通性诊断，是仪器集成体系的一部分。
  - id: P-10017
    name: 仪器发现与驱动匹配
    name_en: Instrument Discovery & Driver Matching
    type: experimental_scene
    rationale: 组件包含设备可见性检查、唯一 POSIX 端口匹配和端口解析，明显属于设备发现与匹配场景。
  - id: P-0003
    name: 实验仪器通信与驱动集成
    name_en: Instrument Communication & Driver Integration
    type: experimental_scene
    rationale: 除了发现外，它还验证端口打开和命令读写能力，紧贴通信驱动集成流程。
  - id: P-10016
    name: 实验室仪器枚举器
    name_en: Laboratory Instrument Enumerator
    type: device_template_tag
    rationale: 该组件会检查设备是否可见于 USB hub 和系统端口，具备显著的枚举器特征。
  - id: P-0005
    name: USB仪器通信接口
    name_en: USB Instrument Communication Interface
    type: device_template_tag
    rationale: 诊断对象围绕 USB 设备连通性与 USB 命令读写展开。
  - id: P-2013
    name: 串行仪器通信接口
    name_en: Serial Instrument Communication Interface
    type: device_template_tag
    rationale: 其诊断范围还包括 POSIX/BSD 串口路径与端口读写，符合串行接口相关模板。
  proposed_new_tags: []
  websearch_evidence:
    used: false
    findings: []
  processing_pass_order:
  - deterministic_local_extraction
  - per_device_semantic_profile
  - agent_conflict_check_and_web_search
  - batch_tag_pass
  - payload_merge
  - render_info_txt
  - validate_and_review
